import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PairProgrammingCRM',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
